# Optional. Without this key, the builder uses its built-in local generator.
OPENROUTER_API_KEY=

# Add the Vibekit token CA after launch. A connected wallet holding at least
# 10,000,000 tokens (1% of the fixed 1B supply) receives 30M AI tokens per UTC day.
VIBEKIT_TOKEN_MINT=
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
